# 단타 RSI 3분할 시스템 — 배포 가이드

## 1. 추가된 파일

| 파일 | 역할 |
|---|---|
| `swing_rsi.py` | 전략 엔진 (유니버스·RSI·3분할 판정·백테스트·Streamlit 화면) |
| `kakao_notify.py` | 카카오톡 '나에게 보내기' 전송 (텔레그램 백업 옵션 내장) |
| `alert_bot.py` | 장중 신호 감시 + 알림 발송 (GitHub Actions가 실행) |
| `get_kakao_token.py` | 카카오 refresh_token 최초 1회 발급 도우미 (로컬 실행) |
| `.github/workflows/swing_alert.yml` | 평일 09:00~15:30, 30분 간격 자동 실행 |
| `swing_positions.json` | 분할 진행 상태 (봇이 자동 갱신·커밋) |
| `app.py` | ⚡ 단타 RSI 3분할 탭 추가 (2번째 탭) |

의존성은 기존 `requirements.txt`로 모두 충족됩니다. 추가 설치 불필요.

---

## 2. 전략 규칙 (구현된 그대로)

**유니버스** — **KOSPI 시총 상위 100종목 + KOSDAQ 시총 상위 10종목 = 110종목**. FinanceDataReader로 매 실행 시 갱신, 실패 시 내장 리스트로 폴백. 우선주·스팩·리츠 자동 제외.

**알림 상한** — 코스닥 신호는 1회 스캔당 최대 5건까지만 발송(코스피는 제한 없음). 우선순위는 손절 → 보유 종목 추가매수·매도 → 신규 진입 순이며, 동순위는 RSI가 극단인 종목이 앞섭니다. 잘린 신호는 사라지지 않고 다음 스캔으로 이월됩니다.

봇 환경변수로 조정 가능: `KOSPI_N`(기본 100) · `KOSDAQ_N`(기본 10) · `KOSDAQ_LIMIT`(기본 5)

**매수 (시드 3분할)**

| 차수 | 조건 | 투입 |
|---|---|---|
| 1차 | RSI(14)가 30을 **하향 돌파**한 봉 | 시드 1/3 |
| 2차 | 1차 매수가 대비 **-5%** | 시드 1/3 |
| 3차 | 1차 매수가 대비 **-10%** | 시드 1/3 |

**매도 (보유 3분할)**

| 차수 | 조건 | 청산 |
|---|---|---|
| 1차 | RSI(14)가 70을 **상향 돌파**한 봉 | 보유 1/3 |
| 2차 | 1차 매도가 대비 **+5%** | 보유 1/3 |
| 3차 | 1차 매도가 대비 **+10%** | 잔량 전량 |

**안전장치** — 평단 대비 **-15%** 이탈 시 전량 청산 신호. 요청안에는 없었으나 넣었습니다. 3차까지 소진한 뒤 계속 하락하면 손절 기준이 없어 무한 보유가 되기 때문입니다. `swing_rsi.py`의 `HARD_STOP_PCT`에서 조정 가능합니다.

---

## 3. 배포 절차

### 3-1. 코드 반영 (즉시 가능)

```
git add .
git commit -m "feat: 단타 RSI 3분할 + 카톡 알림"
git push
```

Streamlit Cloud가 자동 재배포하며 ⚡ 단타 탭이 바로 생깁니다. **카톡 설정 없이도 웹 화면은 정상 동작합니다.**

### 3-2. 카카오 앱 등록 — ✅ 완료됨

앱 `KOSPI 단타알림` (ID 1532737) 생성 및 아래 설정 완료.

- 카카오 로그인 활성화 **ON**
- 로그인 리다이렉트 URI `https://example.com/oauth` 등록
- 동의항목 `카카오톡 메시지 전송(talk_message)` → **선택 동의**
- 클라이언트 시크릿 **활성화 상태** (토큰 요청 시 필수)

키 확인 위치: 카카오 디벨로퍼스 → 앱 → **플랫폼 키** → REST API 키 / 클라이언트 시크릿

### 3-3. 토큰 발급 — ✅ 최초 발급 완료

재발급이 필요할 때(약 2개월 주기)만 아래를 실행합니다.

```
카카오토큰발급.bat  더블클릭   (또는 python get_kakao_token.py)
```

REST API 키와 클라이언트 시크릿을 입력하면 새 refresh_token이 출력됩니다.

### 3-4. GitHub Secrets 등록 ← **직접 하셔야 하는 단계**

저장소 → Settings → Secrets and variables → Actions → **New repository secret**

| Name | 값 |
|---|---|
| `KAKAO_REST_API_KEY` | 플랫폼 키 화면의 REST API 키 |
| `KAKAO_CLIENT_SECRET` | 플랫폼 키 화면의 클라이언트 시크릿 코드 |
| `KAKAO_REFRESH_TOKEN` | 발급받은 refresh token |

### 3-5. 동작 확인

저장소 → Actions → `KOSPI 단타 RSI 카톡 알림` → **Run workflow** → force `1` 입력 후 실행.
로그에 `카카오 전송: 성공`이 뜨고 본인 카톡으로 메시지가 오면 완료입니다.

---

## 4. 운영 시 반드시 알아야 할 제약

**⚠️ 반드시 확인하세요.**

1. **GitHub Actions 크론은 정시에 안 돌 수 있습니다.** 부하에 따라 5~15분 지연이 일상적입니다. 30분봉 전략이라 치명적이진 않지만, 알림 도착 시각과 봉 마감 시각은 다를 수 있습니다.
2. **refresh_token은 약 2개월마다 만료됩니다.** 만료 시 알림이 조용히 끊깁니다. 캘린더에 2개월 주기 알림을 걸어두고 `get_kakao_token.py`를 재실행해 Secret을 갱신하십시오. 로그에 새 토큰이 출력되면 그 값으로 교체하면 됩니다.
3. **가격 데이터는 yfinance(15~20분 지연)입니다.** 실시간 체결가가 아닙니다. 진입가와 알림가에 괴리가 발생합니다. 실시간이 필요하면 키움 OpenAPI+ 또는 한국투자증권 KIS API로 교체해야 하며, 이 경우 로컬 상시 실행 환경이 필요합니다.
4. **30분봉은 최근 60일, 60분봉은 최근 180일까지만** 조회됩니다(yfinance 제한). 백테스트 표본이 작다는 뜻이니 결과를 과신하지 마십시오.
5. **자동 주문은 없습니다.** 알림 → 본인이 직접 주문. 의도된 설계입니다.

---

## 5. 전략에 대한 솔직한 평가

**RSI 30/70 역추세 + 물타기 조합은 상승·횡보장에서 잘 듣고, 추세 하락장에서 크게 깨집니다.** 삼성전자 최근 60일 30분봉 백테스트 결과가 그 성격을 그대로 보여줍니다: 4거래 중 2건 +30%/+18%, 2건 -16%/-18%. 손익은 플러스지만 손실 거래의 크기가 이익 거래에 육박합니다. 3차까지 시드를 다 넣은 상태에서 -15% 손절이 걸리면 **시드 전체의 -15%**입니다.

권고하는 단계적 접근:

**1단계 (지금~2주)** — 알림만 받고 매매하지 않습니다. 신호 도착 시각, 실제 체결 가능 가격, 신호 빈도를 눈으로 검증합니다.

**2단계 (3주차~)** — 실제 시드의 20~30%만 배정해 운용합니다. 종목당 시드가 아니라 **전체 시드 기준 종목당 배분 한도**를 먼저 정하십시오. 시총 50위 중 동시에 5종목에 신호가 뜨면 시드가 순식간에 고갈됩니다. 현재 코드는 종목별 3분할만 관리하고 전체 자금 배분은 관리하지 않습니다.

**3단계 (검증 후)** — 앱 백테스트 탭에서 종목별 승률을 확인해 잘 듣는 종목군만 유니버스로 좁히고, 그때 비중을 올립니다.

다음 개선 후보 (요청 주시면 작업합니다):
- 전체 시드 기준 동시 보유 종목 수 상한 + 자금 배분 관리
- 일봉 추세 필터 (일봉 MA60 아래 종목은 신규 진입 차단 — 하락 추세 물타기 방지)
- 거래대금 급증 필터로 신호 품질 개선
